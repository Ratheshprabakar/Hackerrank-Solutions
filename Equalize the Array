from collections import Counter
def main():
    n = int(input())
    arr = [int(x) for x in input().split()]
    arr_dash = Counter(arr)
    print (n-max(arr_dash.values()))
if __name__=='__main__':
    main()
