def average(array):
    set1=set()
    total=count=0
    for i in arr:
        if i not in set1:
            set1.add(i)
            total=total+i
            count=count+1
    return total/count
if __name__ == '__main__':
